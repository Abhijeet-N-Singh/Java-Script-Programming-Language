// In JavaScript it is called as array
// in python it is called as list
// how to create arrays

// list is an ordered collection of items which is placed inside
// those square brackets are seperated by commas(,). A list may
// contain mixed data types in array.

let fruits = ["apple", "mango", "watermelon"];
let numbers = [1,2,3,4];
let mixed = [1,2,2.3, "string", null, undefined];
console.log(mixed);
console.log(numbers);
console.log(fruits[2]);
let age;// data type called undefined.
console.log(typeof age);
