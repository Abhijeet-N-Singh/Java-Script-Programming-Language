//Function is a group of statements written to 
//perform some specific tasks +, -, %,?

function add(a,b)//==> parameter are nothing but variables
{
    return a+b;
}

let sum=add(2,3);//==> Arguments or values assigned to the parameters
console.log(sum);
